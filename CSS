/* Style the navigation menu */
.topnav {
  overflow: hidden;
  background-color: #333;
  position: relative;
}

/* Hide the links inside the navigation menu (except for logo/home) */
.topnav #myLinks {
  display: none;
}

/* Style navigation menu links */
.topnav a {
  color: white;
  padding: 14px 16px;
  text-decoration:#FFD700;
  font-size: 17px;
  display: block;
}
body{
  background-color: black;
  color: 	#FFD700;
  font-family: space mono;
}


/* Style the hamburger menu */
.topnav a.icon {
  background:#FFD700;
  display: block;
  position: absolute;
  right: 0;
  top: 0;
}

/* Add a grey background color on mouse-over */
.topnav a:hover {
  background-color: white;
  color: 	#FFD700;
}

/* Style the active link (or home/logo) */
.active {
  background-color: #Black;
  color: 	#FFD700;
}
h1{
  font-size:65px;
}
p{
  font-size:25px;
}
#welcome-section{
  height:500px;
  display:flex;
  justify-content:center;
  align-items:center;
  text-align:center;
  background-image: url(https://www.itl.cat/pngfile/big/44-448645_red-space-1920x1080-hd-wallpaper-red-space-desktop.jpg)
  }

.projects{
  height:500px;
  text-align:center;
}

.row{
  display:flex;
  flex-direction:column;
  width:100%;
}
.column{
  display:flex;
  flex-directopm:column
  flex:1
}
